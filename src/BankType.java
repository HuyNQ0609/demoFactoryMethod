/** + Enum là một kiểu dữ liệu đặc biệt của Java được sử dụng
 *          để định nghĩa các tập hợp các hằng số.
 *  + Enum trong java có thể được định nghĩa bên trong hoặc bên
 *          ngoài một lớp, vì nó tương tự như lớp trong java.
 */

public enum BankType {
    TPBank, VietinBank, AgriBank
}
